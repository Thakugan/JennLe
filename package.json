{
  "name": "jenn-le",
  "homepage": "https://jennle.com",
  "description": "My personal website",
  "repository": "https://github.com/thakugan/jenn-le",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "gatsby": "^1.9.247",
    "gatsby-link": "^1.6.40",
    "gatsby-plugin-react-helmet": "^2.0.10",
    "gatsby-plugin-sass": "^1.0.26",
    "react-helmet": "^5.2.0"
  },
  "scripts": {
    "patch":
      "sed -i -e 's/if(c\\.initial) return;/if(!c||c.initial) return;/g' ./node_modules/extract-text-webpack-plugin/index.js",
    "build": "npm run patch && gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "prettier": "^1.12.0"
  }
}
